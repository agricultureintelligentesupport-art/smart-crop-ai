/**
 * Shared shell geometry. One column width keeps the top bar, the scroll
 * content and the tab bar visually locked to the same grid on every screen
 * and on every device (phone-first, centred column on desktop).
 */
export const SHELL_COLUMN = "mx-auto w-full max-w-[560px]";

/** Height of the translucent top bar (mirrors `--app-bar-h`). */
export const SHELL_BAR_HEIGHT = "h-14";
